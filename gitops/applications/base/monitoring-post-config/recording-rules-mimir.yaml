namespace: grafana-mimir
groups:
  - name: recording-rules
    interval: 1m
    rules:
      - record: namespace:pod_cpu_usage:rate
        expr: |
          sum by (namespace) (
            rate(container_cpu_usage_seconds_total{container!="", pod!=""}[5m])
          )