---
apiVersion: v1
kind: Namespace
metadata:
  name: "${ARGOCD_ENV_env_name}"
  annotations:
    name: "${ARGOCD_ENV_env_name}"
---
apiVersion: objectstore.mojaloop.io/v1alpha1
kind: ObjectStore
metadata:
  name: "${ARGOCD_ENV_env_name}-loki-bucket"
  namespace: ${ARGOCD_ENV_env_name}
spec:
  parameters:
    clusterName: ${ARGOCD_ENV_env_name}
    objectStoreProvider: ${ARGOCD_ENV_object_storage_provider}
    bucketName: "loki-${ARGOCD_ENV_env_name}-${ARGOCD_ENV_hyphenated_domain}"
    namespace: ${ARGOCD_ENV_env_name}
    ccK8sProviderName: "kubernetes-provider"
    esoPushSecret: true
    vaultSecretStore: "vault-secret-store"
    vaultSecretPath: "${ARGOCD_ENV_env_name}/loki_bucket_access_key_id"
    s3:
      awsProviderName: "aws-cp-upbound-provider-config"
      bucketRegion: ${ARGOCD_ENV_cloud_region}
      forceDestroy: true
      deletionPolicy: "Delete"
    ceph:
      scK8sProviderName: ${ARGOCD_ENV_sc_provider_config_name}
      storageClassName: "ceph-bucket"
      maxSize: "${ARGOCD_ENV_loki_bucket_storage_size}"
---
apiVersion: objectstore.mojaloop.io/v1alpha1
kind: ObjectStore
metadata:
  name: "${ARGOCD_ENV_env_name}-tempo-bucket"
  namespace: ${ARGOCD_ENV_env_name}
spec:
  parameters:
    clusterName: ${ARGOCD_ENV_env_name}
    objectStoreProvider: ${ARGOCD_ENV_object_storage_provider}
    bucketName: "tempo-${ARGOCD_ENV_env_name}-${ARGOCD_ENV_hyphenated_domain}"
    namespace: ${ARGOCD_ENV_env_name}
    ccK8sProviderName: "kubernetes-provider"
    esoPushSecret: true
    vaultSecretStore: "vault-secret-store"
    vaultSecretPath: "${ARGOCD_ENV_env_name}/tempo_bucket_access_key_id"
    s3:
      awsProviderName: "aws-cp-upbound-provider-config"
      bucketRegion: ${ARGOCD_ENV_cloud_region}
      forceDestroy: true
      deletionPolicy: "Delete"
    ceph:
      scK8sProviderName: ${ARGOCD_ENV_sc_provider_config_name}
      storageClassName: "ceph-bucket"
      maxSize: "${ARGOCD_ENV_tempo_bucket_storage_size}"

---
apiVersion: objectstore.mojaloop.io/v1alpha1
kind: ObjectStore
metadata:
  name: "${ARGOCD_ENV_env_name}-velero-bucket"
  namespace: ${ARGOCD_ENV_env_name}
spec:
  parameters:
    clusterName: ${ARGOCD_ENV_env_name}
    objectStoreProvider: ${ARGOCD_ENV_object_storage_provider}
    bucketName: "velero-${ARGOCD_ENV_env_name}-${ARGOCD_ENV_hyphenated_domain}"
    namespace: ${ARGOCD_ENV_env_name}
    ccK8sProviderName: "kubernetes-provider"
    esoPushSecret: true
    vaultSecretStore: "vault-secret-store"
    vaultSecretPath: "${ARGOCD_ENV_env_name}/velero_bucket_access_key_id"
    s3:
      awsProviderName: "aws-cp-upbound-provider-config"
      bucketRegion: ${ARGOCD_ENV_cloud_region}
      forceDestroy: true
      deletionPolicy: "Delete"
    ceph:
      scK8sProviderName: ${ARGOCD_ENV_sc_provider_config_name}
      storageClassName: "ceph-bucket"
      maxSize: "${ARGOCD_ENV_velero_bucket_storage_size}"
---
apiVersion: objectstore.mojaloop.io/v1alpha1
kind: ObjectStore
metadata:
  name: "${ARGOCD_ENV_env_name}-percona-bucket"
  namespace: ${ARGOCD_ENV_env_name}
spec:
  parameters:
    clusterName: ${ARGOCD_ENV_env_name}
    objectStoreProvider: ${ARGOCD_ENV_object_storage_provider}
    bucketName: "percona-${ARGOCD_ENV_env_name}-${ARGOCD_ENV_hyphenated_domain}"
    namespace: ${ARGOCD_ENV_env_name}
    ccK8sProviderName: "kubernetes-provider"
    esoPushSecret: true
    vaultSecretStore: "vault-secret-store"
    vaultSecretPath: "${ARGOCD_ENV_env_name}/percona_bucket_access_key_id"
    s3:
      awsProviderName: "aws-cp-upbound-provider-config"
      bucketRegion: ${ARGOCD_ENV_cloud_region}
      forceDestroy: true
      deletionPolicy: "Delete"
    ceph:
      scK8sProviderName: ${ARGOCD_ENV_sc_provider_config_name}
      storageClassName: "ceph-bucket"
      maxSize: "${ARGOCD_ENV_percona_bucket_storage_size}"
---
apiVersion: objectstore.mojaloop.io/v1alpha1
kind: ObjectStore
metadata:
  name: "${ARGOCD_ENV_env_name}-audit-bucket"
  namespace: ${ARGOCD_ENV_env_name}
spec:
  parameters:
    clusterName: ${ARGOCD_ENV_env_name}
    objectStoreProvider: ${ARGOCD_ENV_object_storage_provider}
    bucketName: "audit-${ARGOCD_ENV_env_name}-${ARGOCD_ENV_hyphenated_domain}"
    namespace: ${ARGOCD_ENV_env_name}
    ccK8sProviderName: "kubernetes-provider"
    esoPushSecret: true
    vaultSecretStore: "vault-secret-store"
    vaultSecretPath: "${ARGOCD_ENV_env_name}/audit_bucket_access_key_id"
    s3:
      awsProviderName: "aws-cp-upbound-provider-config"
      bucketRegion: ${ARGOCD_ENV_cloud_region}
      forceDestroy: true
      deletionPolicy: "Delete"
    ceph:
      scK8sProviderName: ${ARGOCD_ENV_sc_provider_config_name}
      storageClassName: "ceph-bucket"
      maxSize: "${ARGOCD_ENV_audit_bucket_storage_size}"
---
apiVersion: objectstore.mojaloop.io/v1alpha1
kind: ObjectStore
metadata:
  name: "${ARGOCD_ENV_env_name}-report-bucket"
  namespace: ${ARGOCD_ENV_env_name}
spec:
  parameters:
    clusterName: ${ARGOCD_ENV_env_name}
    objectStoreProvider: ${ARGOCD_ENV_object_storage_provider}
    bucketName: "report-${ARGOCD_ENV_env_name}-${ARGOCD_ENV_hyphenated_domain}"
    namespace: ${ARGOCD_ENV_env_name}
    ccK8sProviderName: "kubernetes-provider"
    esoPushSecret: true
    vaultSecretStore: "vault-secret-store"
    vaultSecretPath: "${ARGOCD_ENV_env_name}/report_bucket_access_key_id"
    s3:
      awsProviderName: "aws-cp-upbound-provider-config"
      bucketRegion: ${ARGOCD_ENV_cloud_region}
      forceDestroy: true
      deletionPolicy: "Delete"
    ceph:
      scK8sProviderName: ${ARGOCD_ENV_sc_provider_config_name}
      storageClassName: "ceph-bucket"
      maxSize: "${ARGOCD_ENV_report_bucket_storage_size}"
      bucketPolicy:
        Version: "2012-10-17"
        Statement:
          # Report bucket is publicly readable
          - Effect: Allow
            Principal: "*"
            Action: s3:GetObject
            Resource:
              - arn:aws:s3:::report-${ARGOCD_ENV_env_name}-${ARGOCD_ENV_hyphenated_domain}/*
              - arn:aws:s3:::report-${ARGOCD_ENV_env_name}-${ARGOCD_ENV_hyphenated_domain}
      bucketLifecycle:
        Rules:
          # Automatically delete objects in the report bucket after 7 days
          - ID: ExpireAfter7Days
            Status: Enabled
            Prefix: ""
            Expiration:
              Days: 7

---
apiVersion: objectstore.mojaloop.io/v1alpha1
kind: ObjectStore
metadata:
  name: "${ARGOCD_ENV_env_name}-vault-backup-bucket"
  namespace: ${ARGOCD_ENV_env_name}
spec:
  parameters:
    clusterName: ${ARGOCD_ENV_env_name}
    objectStoreProvider: ${ARGOCD_ENV_object_storage_provider}
    bucketName: "vault-backup-${ARGOCD_ENV_env_name}-${ARGOCD_ENV_hyphenated_domain}"
    namespace: ${ARGOCD_ENV_env_name}
    ccK8sProviderName: "kubernetes-provider"
    esoPushSecret: true
    vaultSecretStore: "vault-secret-store"
    vaultSecretPath: "${ARGOCD_ENV_env_name}/vault_backup_bucket_access_key_id"
    s3:
      awsProviderName: "aws-cp-upbound-provider-config"
      bucketRegion: ${ARGOCD_ENV_cloud_region}
      forceDestroy: true
      deletionPolicy: "Delete"
    ceph:
      scK8sProviderName: ${ARGOCD_ENV_sc_provider_config_name}
      storageClassName: "ceph-bucket"
      maxSize: "${ARGOCD_ENV_vault_backup_bucket_storage_size}"
