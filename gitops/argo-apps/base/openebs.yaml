apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ${ARGOCD_ENV_sc_openebs_app_name}
  namespace: ${ARGOCD_ENV_sc_app_namespace}
  annotations:
    argocd.argoproj.io/sync-wave: ${ARGOCD_ENV_sc_openebs_sync_wave}

  finalizers:
    - resources-finalizer.argocd.argoproj.io

spec:
  project: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 20
      backoff:
        duration: 10s
        maxDuration: 3m0s
        factor: 2
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
  destination:
    server: "https://kubernetes.default.svc"
    namespace: "${ARGOCD_ENV_sc_openebs_namespace}"

  source:
    repoURL: ${ARGOCD_ENV_argocd_repo_url}
    targetRevision: ${ARGOCD_ENV_sc_application_gitrepo_tag}
    path: gitops/applications/base/openebs
    plugin:
      name: envsubst
      env:
        - name: "openebs_namespace"
          value: "${ARGOCD_ENV_sc_openebs_namespace}"

        - name: "openebs_helm_version"
          value: "${ARGOCD_ENV_sc_openebs_helm_version}"

